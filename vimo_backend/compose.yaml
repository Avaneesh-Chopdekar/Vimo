services:
  db_postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=vimo_db'
      - 'POSTGRES_PASSWORD=vimo_password'
      - 'POSTGRES_USER=vimo_user'
    ports:
      - '5432:5432'
    volumes:
        - 'postgres_data:/var/lib/postgresql/data'

volumes:
  postgres_data: